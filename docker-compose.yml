version: "3.8"
services:
  database:
    container_name: database
    image: postgres
    ports:
      - "5432:5432"
    volumes:
      - ./database:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=dishRankDB
      - POSTGRES_USER=surmistry
      - POSTGRES_PASSWORD=password
